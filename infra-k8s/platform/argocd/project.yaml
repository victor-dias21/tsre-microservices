apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: tsre-platform
  namespace: argocd
spec:
  description: TSRE platform and application project
  sourceRepos:
    - '*'
  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
